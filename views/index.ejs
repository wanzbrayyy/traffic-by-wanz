<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Web Screenshot Tool</title>
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
  <style>
    * { margin:0; padding:0; box-sizing:border-box; font-family: 'Segoe UI', system-ui, sans-serif; }
    body { background: linear-gradient(120deg, #1a237e, #4a148c); color: white; min-height: 100vh; padding: 20px; }
    .container { max-width: 900px; margin: 0 auto; }
    header { text-align: center; padding: 30px 0; }
    header h1 { font-size: 2.4rem; margin-bottom: 10px; }
    header p { opacity: 0.9; }
    .card { background: rgba(0,0,0,0.3); border-radius: 16px; padding: 25px; margin: 20px 0; backdrop-filter: blur(10px); }
    .form-group { margin-bottom: 20px; }
    label { display: block; margin-bottom: 8px; font-weight: 600; }
    input, select, button { width: 100%; padding: 14px; border: none; border-radius: 10px; font-size: 1rem; }
    button { background: #ff5722; color: white; font-weight: bold; cursor: pointer; transition: 0.3s; }
    button:hover { background: #e64a19; transform: translateY(-2px); }
    .result { margin-top: 30px; }
    .error { background: #d32f2f; padding: 15px; border-radius: 10px; margin: 20px 0; }
    .screenshot-box { background: rgba(255,255,255,0.1); border-radius: 12px; padding: 20px; text-align: center; margin-top: 20px; }
    .screenshot-box img { max-width: 100%; border-radius: 8px; box-shadow: 0 8px 24px rgba(0,0,0,0.4); margin-top: 15px; }
    footer { text-align: center; margin-top: 40px; opacity: 0.7; font-size: 0.9rem; }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <h1><i class="fas fa-camera"></i> Web Screenshot Tool</h1>
      <p>Masukkan URL, pilih perangkat, dan dapatkan screenshot langsung</p>
    </header>

    <div class="card">
      <form method="POST" action="/screenshot">
        <div class="form-group">
          <label for="url"><i class="fas fa-link"></i> URL Website</label>
          <input type="url" id="url" name="url" placeholder="https://example.com" required>
        </div>
        <div class="form-group">
          <label for="device"><i class="fas fa-tablet-alt"></i> Tampilan Perangkat</label>
          <select id="device" name="device">
            <option value="desktop">Desktop (1920x1080)</option>
            <option value="mobile">Mobile (375x667)</option>
          </select>
        </div>
        <button type="submit">
          <i class="fas fa-shutter"></i> Ambil Screenshot
        </button>
      </form>
    </div>

    <% if (result && result.error) { %>
      <div class="card error">
        <i class="fas fa-exclamation-circle"></i> <%= result.error %>
      </div>
    <% } else if (result && result.screenshotUrl) { %>
      <div class="card screenshot-box">
        <h3><i class="fas fa-image"></i> Hasil Screenshot (<%= result.device %>)</h3>
        <p>URL: <strong><%= result.url %></strong></p>
        <img src="<%= result.screenshotUrl %>" alt="Screenshot">
      </div>
    <% } %>

    <footer>
      <p>© 2025 Web Screenshot Tool • Gambar disimpan di Cloudinary</p>
    </footer>
  </div>
</body>
</html>